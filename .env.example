# =============================================================================
# OLLAMA API GATEWAY - ENVIRONMENT CONFIGURATION (EXAMPLE)
# =============================================================================
# Copy this file to create your own configuration:
#   cp .env.example .env
#
# Then update the values according to your environment.
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_DB=ollama_api
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Database URL for Backend (Docker environment)
# Format: postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/ollama_api

# Database URL for Local Development (non-Docker)
# Uncomment this line if running backend locally without Docker:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ollama_api

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# IMPORTANT: Generate a secure key for production!
# Run: openssl rand -hex 32
SECRET_KEY=your-super-secret-key-change-this-in-production

# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Server host and port
HOST=0.0.0.0
PORT=8000

# -----------------------------------------------------------------------------
# Ollama Configuration
# -----------------------------------------------------------------------------
# For Docker on macOS/Windows: use host.docker.internal
# For Docker on Linux: use host.docker.internal or 172.17.0.1
# For local development: use localhost or your Ollama server IP
OLLAMA_BASE_URL=http://host.docker.internal:11434

# -----------------------------------------------------------------------------
# Demo API Keys (Testing Only - Remove in Production)
# -----------------------------------------------------------------------------
# Leave empty in production to disable demo keys
# Generate secure keys with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
DEMO_ADMIN_KEY=demo-admin-key-12345
DEMO_USER_KEY=demo-user-key-67890

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# API URL for the frontend to connect to backend
# For Docker: use http://localhost:8000
# For production: use your actual API domain (e.g., https://api.yourdomain.com)
VITE_API_URL=http://localhost:8000

# Frontend port
FRONTEND_PORT=3000

# -----------------------------------------------------------------------------
# Docker Configuration
# -----------------------------------------------------------------------------
# Platform architecture
# Use linux/amd64 for Apple Silicon (M1/M2/M3) compatibility
# Use linux/arm64 for native ARM64 builds
DOCKER_PLATFORM=linux/amd64

# -----------------------------------------------------------------------------
# Production Configuration Example (Uncomment and configure for production)
# -----------------------------------------------------------------------------
# SECRET_KEY=<generate-with-openssl-rand-hex-32>
# POSTGRES_PASSWORD=<use-a-strong-password>
# POSTGRES_PORT=5432
# DEMO_ADMIN_KEY=  # Leave empty to disable
# DEMO_USER_KEY=    # Leave empty to disable
# VITE_API_URL=https://api.yourdomain.com
# OLLAMA_BASE_URL=https://ollama.yourdomain.com
# DATABASE_URL=postgresql+asyncpg://username:password@your-db-host:5432/ollama_api
