# =============================================================================
# BACKEND ENVIRONMENT CONFIGURATION
# =============================================================================
# NOTE: This file is deprecated. The project now uses a single .env file
# in the root directory for all services.
#
# Please use the root .env file instead:
#   /path/to/project/.env.example
#
# Copy the root .env.example to .env:
#   cp ../.env.example ../.env
# =============================================================================

# For backward compatibility, you can still use this file for local development
# without Docker. However, we recommend using the root .env file.

# Security Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Database Configuration (for local development without Docker)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ollama_api

# Demo API Keys (optional - for testing only)
# Generate secure keys with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
DEMO_ADMIN_KEY=demo-admin-key-12345
DEMO_USER_KEY=demo-user-key-67890

# Ollama Configuration (for local development)
OLLAMA_BASE_URL=http://localhost:11434

# Server Configuration
HOST=0.0.0.0
PORT=8000
